# GitHub Pages 部署设置指南

## 已完成的配置

### 1. Next.js 静态导出配置
- ✅ 已配置 `next.config.mjs` 支持静态导出
- ✅ 设置 `output: 'export'`
- ✅ 配置 `trailingSlash: true`
- ✅ 设置输出目录为 `out`
- ✅ 配置生产环境的 `basePath` 和 `assetPrefix`

### 2. GitHub Actions 工作流
- ✅ 创建了 `.github/workflows/deploy.yml`
- ✅ 配置自动构建和部署流程
- ✅ 支持推送到 main 分支时自动部署

### 3. 构建脚本
- ✅ 添加了 `export` 脚本到 `package.json`

## 重要限制和注意事项

### 🚨 API 路由无法在 GitHub Pages 运行
GitHub Pages 只支持静态文件托管，以下 API 路由将无法工作：
- `/api/stream-model` - AI 模型流式响应
- `/api/generate-network` - 知识网络生成
- `/api/agent-network` - 智能体网络
- `/api/mock-stream` - 模拟流
- `/api/test` - 测试接口

### 🔧 需要的功能修改建议

#### 选项 1: 纯静态展示版本
1. 移除或禁用依赖 API 的功能
2. 创建静态演示数据
3. 添加说明页面解释功能限制

#### 选项 2: 使用外部 API 服务
1. 将 API 部署到其他平台（如 Vercel、Netlify）
2. 修改前端配置指向外部 API 地址
3. 保持 GitHub Pages 只托管静态前端

#### 选项 3: 完全平台部署
1. 使用 Vercel 直接部署整个 Next.js 应用
2. 保留所有 API 功能
3. 无需 GitHub Pages

## 手动部署步骤

1. **在 GitHub 仓库设置中启用 Pages**
   - 进入 Settings → Pages
   - Source 选择 "GitHub Actions"

2. **推送代码到 main 分支**
   - GitHub Actions 将自动触发构建和部署

3. **访问网站**
   - 部署完成后访问: `https://xiangshb.github.io/scientific-knowledge-navigation/`

## 测试构建

在本地测试静态构建：

```bash
cd scientific-knowledge-navigation
npm run build
# 检查 out 目录中的静态文件
```

## 推荐解决方案

考虑到项目包含大量 API 功能，**推荐使用 Vercel 进行完整部署**以保留所有功能。GitHub Pages 仅适用于纯静态展示。
